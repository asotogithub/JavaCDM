'use strict';

describe('Service: ErrorHandler', function () {
    var errorHandler;

    beforeEach(module('uiApp'));

    beforeEach(
        inject(function (ErrorHandler) {
            errorHandler = ErrorHandler;
        })

    );
});
